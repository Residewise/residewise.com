{{ form_start(form) }}
<div class="input-group">
    {{ form_widget(form.keyword, {
        'attr' : {
            'class' : bgColor,
            placeholder: 'search'|trans
        },
    }) }}
</div>
{{ form_end(form) }}
