{{ form_start(form) }}
<div class="input-group">
    {{ form_widget(form.keyword) }}
    <button type="submit" class="btn btn-primary">
        <span class="mdi mdi-magnify fs-3"></span>
    </button>
</div>
{{ form_end(form) }}
