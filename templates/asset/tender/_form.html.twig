{{ form_start(tenderForm) }}
    {{ form_row(tenderForm.startAt) }}
    {{ form_row(tenderForm.endAt) }}
    {{ form_row(tenderForm.minimumBid) }}
<div class="text-end">
    <button type="submit" class="btn btn-primary">
        {{ 'save'|trans }}
    </button>
</div>
{{ form_end(tenderForm) }}
