{% extends 'base.html.twig' %}

{% block title %}New Asset{% endblock %}

{% block body %}
    <div class="container">
        <turbo-frame id="asset-form">
            <div class="row justify-content-center mt-3">
                <div class="col-md-8">
                    {{ include('asset/_form.html.twig') }}
                </div>
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-body">
                        ADS
                        </div>
                    </div>
                </div>
            </div>
        </turbo-frame>
    </div>
{% endblock %}
