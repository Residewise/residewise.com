{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center mt-3">
            <div class="col-md-4">
                <div class="card">
                    <div class="card-body">
                        <h5>{{ 'set-password'|trans }}</h5>
                        {{ include('security/_password-form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
