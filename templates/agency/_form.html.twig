{{ form_start(agencyForm) }}
    {{ form_row(agencyForm.logo) }}
    {{ form_row(agencyForm.title) }}
    {{ form_row(agencyForm.description) }}
    {{ form_row(agencyForm.address) }}
<div class="text-end">
    <button type="submit" class="btn btn-primary">{{ 'save'|trans }}</button>
</div>
{{ form_end(agencyForm) }}
